apiVersion: batch/v1
kind: Job
metadata:
  name: malware-analysis-job
spec:
  template:
    spec:
      containers:
      - name: static-analysis
        image: ubuntu:22.04
        command: ["echo", "Analyzing malware sample..."]
      restartPolicy: Never
  backoffLimit: 3